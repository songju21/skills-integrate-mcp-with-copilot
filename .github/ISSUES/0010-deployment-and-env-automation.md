---
title: Add deployment scripts and environment management
labels: infra, devops
---

Add deployment helpers, `docker-compose` and scripts to manage environment
variables and common post-deploy tasks.

Suggested tasks:
- Add `docker-compose.yml` for local development
- Add deploy scripts and docs for staging/production
- Automate `.env` setup and secrets management guidance
